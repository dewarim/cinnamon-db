package cinnamon

/**
 * Determines which flag of the relation type object to use to determine if
 * relations should be copied over to a new OSD.
 */
public enum CopyRelationMode {

    COPY,
    VERSION

}