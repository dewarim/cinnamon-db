import cinnamon.FolderType
import cinnamon.global.Constants

fixture{
    
    defaultFolderType(FolderType, name:Constants.FOLDER_TYPE_DEFAULT)
    folderReferenceType(FolderType, name:Constants.FOLDER_TYPE_REFERENCE)
    
}
